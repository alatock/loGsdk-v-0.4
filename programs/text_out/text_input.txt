label. #main
    label. #init
    transf. r0 r1
    transf. r0 r2
    label. #code
        input. "r" r1 P3
        out. "r" r1 P5
        if. r1 $ '0' #init
        label. #loop
            input. "r" r2 P4
            if. r2 = r1 #write
        go. #loop
    label. #write
    transf. r2 r3
    if. r3 $ '64' #end
    out. "r" r3 P3
    out. "r" r3 P5
    go. #init
label. #end
    out. "n" '1' P2
halt.