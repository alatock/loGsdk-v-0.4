label. #main
    ramw. "c" &10 [108, 111, 103, 116, 120, 116, 32, 98, 101, 116, 97] #c 10 по 21
    assig. r1 '11' #кол во символов
    assig. r2 '10' #адрес первого байта
    global. "text_out_cl.txt"
    label. #init
    ramw. "c" &10 [101, 110, 116, 114, 32, 116, 101, 120, 116] #c 10 по 19
    assig. r1 '9' 
    assig. r2 '10'
    global. "text_out_cl.txt"
    label. #loop
        global. "char_listen_cl.txt"
        if. r6 $ '49' #writefile
        if. r6 $ '50' #readfile
        if. r6 $ '51' #break
        go. #loop
    label. #writefile
        assig. r4 '50'
        assig. r5 '0'
        global. "line_listen_cl.txt"
        go. #init
    label. #readfile
        assig. "r" r4 '0'
        assig. "r" r2 '50'
        transf. r5 r1
        out. "r" r1 P5
        out. "r" r2 P5
        global. "text_out_cl.txt"
        go. #init
label. #break
halt.