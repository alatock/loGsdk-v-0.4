label. #main
    label. #init
    assig. r1 '0'
    assig. r2 '0'
    assig. r4 '0'
        input. "r" r1 P3
        out. "r" r1 P5
    label. #wait
        if. r4 $ '710' #ret
        math. "i" r4 '1'
        go. #wait
    label. #ret
        input. "r" r2 P3
        if. r1 = r2 #write
        assig. r4 '0'
        go. #init
    label. #write
    transf. r2 r3
    if. r3 $ '64' #end
    out. "r" r3 P3
    out. "r" r3 P5
    go. #init
label. #end
    out. "n" '1' P2
halt.